import styled from "styled-components";

const Input = styled.input`
  border: 1px solid var(--color-grey-300);
  background-color: var(--color-grey-0);
  border: 0.8rem 1.2rem;
  box-shadow: var(--shadow-sm);
  padding: 5px;
  border-radius: 8px;
`;

export default Input;
